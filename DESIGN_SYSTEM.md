# 🎨 Cell Kitchen 4.0 旗艦設計系統規格書 (Design System Spec)

> **設計哲學**：採用高質感韓系極簡風格 (Korean Creamy Minimalist UI)。結合奶油白、暖陽黃、珊瑚紅與薄荷綠防焦慮視覺，搭配大圓角、微妙陰影與動態微互動，打造令人驚豔的現代化 UI/UX。

---

## 🎨 1. 色彩權威矩陣與膠囊色票 (Color System & Capsule Tokens)

| 色彩角色 / 標籤 | 色碼 (HEX) | 用途與說明 |
| :--- | :--- | :--- |
| **輕盈奶灰背景 (Base)** | `#F5F5F5` | 主背景色，營造乾淨質感 |
| **表面邊框/柔灰 (Surface & Warm Gray)** | `#ECECEC` | 無庫存膠囊背景色 (`.capsule.out-stock`)、卡片邊框 (`.color-border`) |
| **輔助灰/購物車原始 (Muted & Cart Default)** | `#B3B3B3` | 輔助文字、預設灰色購物車 Icon (`.split-right .cart-icon-default`) |
| **深炭黑主文字色 (Main Text)** | `#333333` | 頁面主要標題、內文與核心按鈕色 (`.color-text-main`) |
| **珊瑚朱紅 (Coral Red Accent)** | `#E16262` | Ariel 人物色、刪除食材按鈕、爆表警告標籤 |
| **暖陽金黃 (Golden Yellow Accent)** | `#FFCA60` | Bebe 人物色、高亮標示、重點 Call-to-Action 主按鈕 (`.btn-primary.accent`) |
| **柔綠湖藍 (Mint Cyan Active)** | `#7DC7CC` | Jason 人物色、有庫存膠囊選取態 (`#EAF6F7` 底 ＋ `#7DC7CC` 框)、購物車選取圓底背景色 |

---

## 📐 2. 狀態標籤與邏輯 (Status Indicators)

### 2.1 全天總預算三色狀態標籤 (Daily Budget 3-Color Status Tags)
1. 🟢 **綠色 (Safe & Achieved)**：數值在全天總預算合理安全區間內（未爆表/已達標）。
2. ⚫️ **黑色 (Under Target)**：單餐累積數值未達到全天目標。
3. 🔴 **紅色 (Over Budget)**：全天總累積熱量/營養素超過全天上限。

---

### 2.2 第一頁【備料計算頁】膠囊狀態規格 (Page 1 Single Capsule)
> **職責**：專注「料理選取與下鍋計算」，畫面 **100% 隱藏購物車圖示**，保持極致乾淨。

| 狀態名稱 (State) | 業務定義 (Business Meaning) | UI 視覺樣式 (Visual Specs) | 觸控互動行為 (Interaction) |
| :--- | :--- | :--- | :--- |
| **Default (常態)** | **有庫存 ＋ 未選取** | 純白背景 (`#FFFFFF`)、1.5px 淺灰實線邊框 (`#E5E7EB`)、深灰字 (`#1F2937`) | 短按 (Tap) ➔ 切換為 `Selected` |
| **Selected (選取)** | **有庫存 ＋ 已選取** | 淡薄荷綠背景 (`#E8F8F2`)、1.5px 薄荷綠邊框 (`#5BE3AB`)、深綠字 (`#065F46`) | 短按 (Tap) ➔ 切換為 `Default` |
| **Disabled (停用)** | **無庫存** | 暖柔灰背景 (`#EBE9E4`)、1px 細灰邊框 (`#D6D3CC`)、淡灰字 (`#9A958C`) | **完全無效 (Unclickable)** |

---

### 2.3 第三頁【智慧冰箱頁】分割式膠囊狀態規格 (Page 3 Split Capsule)
> **職責**：專注「庫存管理與賣場採買」，採用 **雙觸控分割膠囊 (Split Capsule)**。

* **左半部（70% 面積）**：控制「庫存狀態 (Stock Toggle)」。
  - `Default (無庫存)` ➔ 極淺暖柔灰底 (`#F5F3EF`) ＋ 極淺線框 (`#F0EEE9`，比 `#E5E7EB` 更淡) ＋ 淡灰字 (`#9A958C`)
  - `Selected (有庫存)` ➔ 純白底 (`#FFFFFF`) ＋ 1.5px 淺灰實線邊框 (`#E5E7EB`) ＋ 深灰字 (`#1F2937`)
* **右半部（30% 面積）**：控制「採買清單狀態 (Cart Toggle)」。
  - `Default (沒有要買)` ➔ 原色淡灰色線條購物車 🛒 (`#9A958C`)
  - `Selected (要買已放入)` ➔ 薄荷綠圓底 ＋ 白色購物車 🛒 (`#5BE3AB`)

#### 4 大組合狀態矩陣 (2x2 Combination Matrix)：
1. **State 1 (無庫存 ＋ 沒有要買)**：`左 Default (暖柔灰) | 右 Default (灰線條 🛒)`
2. **State 2 (無庫存 ＋ 要買已放入)**：`左 Default (暖柔灰) | 右 Selected (綠圓底白 🛒)`
3. **State 3 (有庫存 ＋ 沒有要買)**：`左 Selected (純白) | 右 Default (灰線條 🛒)`
4. **State 4 (有庫存 ＋ 想囤貨已放入)**：`左 Selected (純白) | 右 Selected (綠圓底白 🛒)`

#### B. 用餐人數膠囊 (Diners Capsules)
| 狀態 | 視覺表現 | 樣式定義 | 互動行為 |
| :--- | :--- | :--- | :--- |
| **已勾選 (Active)** | **淡綠色背景**、綠色邊框、深綠文字 | `background: #E8F5E9; border-color: #81C784; color: #1B5E20;` | 參與本次備料計算與卡片展開 |
| **未勾選 (Inactive)** | **純白背景**、淺灰實線邊框、深灰文字 | `background: #FFFFFF; border-color: #E5E7EB; color: #1F2937;` | 不參與本次計算，04 卡片不展開 |

#### C. 核心與功能操作按鈕 (CTA & Actions)
| 按鈕名稱 | 視覺特徵 | 樣式規格 (HEX) | 用途說明 |
| :--- | :--- | :--- | :--- |
| **`[ 🧮 計算 ]`** | 暖陽黃背景、純白文字、圓角 | `background: #F59E0B; color: #FFF;` | 觸發 03 大白板與 04 卡片計算 |
| **`[ ➕ 加食材 ]`** | 純白底、深灰文字、淺灰邊框 | `background: #FFF; color: #1F2937; border: 1px solid #E5E7EB;` | 開啟搜尋食材抽屜 |
| **`[ 🔄 重算 ]`** | 深霧灰背景、純白文字 | `background: #1F2937; color: #FFF;` | 重新依當前數值計算 |
| **`[ 📝 紀錄 ]`** | 暖陽橘/金色背景、純白文字、陰影 | `background: #F59E0B; color: #FFF;` | 寫入今日飲食紀錄 |
| **`[ - ] [ + ]`** | 圓角小按鈕、淺灰邊框、微縮動畫 | `padding: 4px 10px; border-radius: 8px;` | 智慧微調個人克數 (1g/5g/10g) |

---

## 📐 3. UI 元件與版面佈局規範 (UI Layout & Components Spec)

> **黃金首頁原則 (Above the Fold Rule)**：省去頂部大 Header Banner，100% 把黃金首頁版面留給【料理選擇】與【防焦慮安全條】，備料零滑動開頁即用！

### 3.1 第一頁：【備料計算器 View】
1. **極簡 Header**：左側標題 `CELL KITCHEN 4.0`，右側 `[ 🔄 刷更新 ]` 按鈕。
2. **頂部三主頁 Tabs**：`[ 🍳 備料計算器 ]` | `[ 📊 今日紀錄 ]` | `[ 🧊 智慧冰箱 ]`
3. **01 DISH 選擇料理**：韓系圓角下拉選單（時間時段自動置頂排序）。
4. **02 INGREDIENTS 食材選取 (同排標題按鈕)**：
   - 標題列右側按鈕：`02 SELECT INGREDIENTS 食材選取`  `[ ➕ 更多/新增食材 ]`  `[ 👁️ 隱藏無庫存膠囊 (快篩) ]`
   - **大總庫模糊搜尋 ＋ 5秒現場新增**：
     - *狀況 1 (大總庫有食材)*：搜尋選取後寫入 `dishes.json` 推薦清單並加入膠囊。
     - *狀況 2 (大總庫無食材)*：5秒填寫名稱/分類/單位，同步寫入 `ingredients.json` 與 `dishes.json`。
   - 分類膠囊：蛋白質、蔬菜、碳水、醬料（無 Icon 純文字）。
   - **無庫存膠囊視覺**：`[ 楓糖漿 🛒 ]` (與一般膠囊一致為白底灰框，點擊整顆膠囊切換購物車綠色徽章，寫入/移出賣場待買清單；長按 1 秒彈出營養資訊卡片)。
   - **長按膠囊 1秒 (Long Press)**：無庫存食材依然可長按檢視 **「食材詳細資料與營養標示卡片」**，展示品牌與每 100g 數據表格，並提供 **`[ 📸 拍照拍攝外包裝營養標示 (AI 自動辨識覆寫) ]`** 按鈕！
5. **DINERS & CALCULATE 人數與計算橫列**：
   - 先選食材、後確認人數：`[ 😊 Bebe ✓ ]  [ ❤️ 樂樂 ]  [ 🪨 Jason ]`  **`[ 🧮 計算 ]`** *(預設只勾選 Bebe)*
6. **03 PORTIONS 全家備料大白板**：
   - 正確呈現 `包` / `顆` / `條` / `g` 動態單位。
   - **大白板右上方/下方雙工具按鈕**：
     - **`[ 📋 複製全家備料與食譜 ]`** (一鍵複製全家備料 ＋ 特調醬料 ＋ SOP 步驟至剪貼簿)
     - **`[ 📖 檢視食譜 SOP 步驟 v ]`** (點擊動態展開 SOP，特調醬料依勾選人數做倍率乘算)
7. **04 MEMBER CARDS 全家成員卡片 (垂直全展開)**：
   - 卡片抬頭完整呈現五大指標全天總預算三色標籤：  
     `🟢 熱量: 432k | 🟢 蛋白: 35g | 🟢 碳水: 45g | 🟢 脂: 15g | 🟢 鈉: 540mg`
   - 各食材 `[ - ] 80g [ + ]` 5g 步進微調鈕，名稱後方帶有淺灰錨定推薦值 `(100g)` 作為調配參考。
8. **底部常駐浮動三控制鈕 (計算後浮現)**：
   - **`[ ➕ 加食材 ]`** (開啟 75% 同排搜尋抽屜) | **`[ 🔄 重算 ]`** | **`[ 📝 紀錄 ]`** (一鍵寫入第二頁今日紀錄，並跳出微提示詢問 **`「是否順便將用完之食材標示為無庫存？」`** 連動第三頁扣減庫存)。

---

### 3.2 第二頁：【今日紀錄與進度看板 (Tracker View)】
1. **統一 Header 與 Tabs**：左側標題 `CELL KITCHEN 4.0`，右側 `[ 🔄 刷更新 ]`，頁籤切換至 `[ 📊 今日紀錄 ]`。
2. **日期與成員切換列**：
   - `[ ◀️ 2026-08-12 今天 v ▶️ ]`（可切換翻閱歷史日期，載入當天結算狀態）。
   - `[ 😊 Bebe (預設) ] [ ❤️ 樂樂 ] [ 🪨 Jason ]`（一鍵切換看全家個人的全天達標進度）。
3. **全天五大健康指標達標進度條 (Daily Target Progress Bars)**：
   - 完整呈現：熱量 (kcal)、蛋白質 (g)、碳水 (g)、脂肪 (g)、鈉 (mg)。
   - 三色剩餘額度標籤（如：`🟢 剩 55 kcal` / `🟢 剩 4.2g 蛋白`）。
4. **當日用餐時間軸 (Meal Timeline Log)**：
   - 依時間順序列出：早餐、午餐、晚餐、點心/外食。
   - 每餐卡片包含：餐點名稱、時間、五大數據、食材明細與 `[ 🗑️ 刪除 ]` 按鈕。
5. **底部常駐控制鈕 (Bottom Action Bar)**：
   - **`[ 📷 拍照 AI 補記外食 ]`** ｜ **`[ 💬 文字/語音補記 ]`**

---

### 3.3 第三頁：【智慧冰箱 View (Pantry View)】
1. **統一 Header 與 Tabs**：左側標題 `CELL KITCHEN 4.0`，右側 `[ 🔄 刷更新 ]`，頁籤切換至 `[ 🧊 智慧冰箱 ]`。
2. **主畫面：01 🧊 冰箱食材庫存 CheckList (Pantry Stock)**：
   - **第一層 (存放區域)**：四大區塊 ➔ **`❄️ 冷藏區`** ｜ **`🧊 冷凍區`** ｜ **`🏠 常溫區`** ｜ **`🧻 生活雜項區`** *(常購廚房紙巾/洗碗精/垃圾袋等非食品，完全與飲食隔離)*。
   - **第二層 (營養素分類)**：食品區塊內依 4 大營養素分小類：`1. 蛋白質` ｜ `2. 蔬菜` ｜ `3. 碳水` ｜ `4. 醬料/調味料`。
   - **膠囊樣式與庫存連動**：
     - **白底綠框** `[ 蛋 ]` 表示有庫存（`border: 1.5px solid #10B981`，高辨識度，與第一頁食材選取膠囊完全一致）。
     - **淺灰底灰框** `[ 牛肉片 🛒 ]` 表示無庫存，點擊膠囊切換寫入/移出賣場待買清單。
   - **長按食材膠囊 1 秒 Modal (食品)**：顯示每 100g 營養數據與 AI 拍照覆寫按鈕，底部提供 **`[ 🗑️ 永久刪除此食材 (不再回購) ]`**。
   - **長按雜項膠囊 1 秒 Modal (非食品)**：顯示實體照片 `photoUrl`、品牌 `brand`、參考價格 `price` ＋ **`自動平均單價換算 (如：$369/12捲 ➔ 平均 $30.8/捲)`**、常用通路 `store` 與 **`[ 📤 一鍵複製代購訊息至 LINE ]`** 按鈕，底部提供 **`[ 🗑️ 永久刪除此雜項 (不再回購) ]`**。*(未來UI預留：近 3 次歷史價格比價清單 `priceHistory` 與 🟢/🔴 智能高低價標籤)*。
3. **手機底部常駐雙控制鈕 (Bottom Floating Action Bar)**：
   - **`[ 🛒 賣場採買清單 ]`**：點擊跳出 **滿版獨立全螢幕採買視窗** (100% 鎖定防誤觸)。
     - **賣場通路 Filter 膠囊**：頂部提供 `[ 🌐 All 全部 (預設) ]` ｜ `[ 🔵 全聯 ]` ｜ `[ 🔴 Costco ]` 一鍵過濾。
     - **店家 Icon 智能去冗餘**：**僅在 `[ All 全部 ]` 模式下顯示店家 Icon 標籤**；切換至 `全聯` 或 `Costco` 特定模式時，品項尾端之店家 Icon **自動隱藏**。
     - **採買視窗內部自動分類**：清楚分區列出 `🥗 食材類` 與 `🧻 生活雜項類`，並提供 `[ ➕ 臨時新增其他雜項... ]` 輸入框。
     - **iOS 手勢返回**：支援 **「螢幕左緣向右滑動 (Edge Swipe Right)」** 秒返回智慧冰箱。
     - **底部 3 控制鈕**：`[ ⬅️ 返回智慧冰箱 ]` (左一黃金位置) ｜ **`[ 🧹 清除並自動入庫 (標示為有庫存) ]`** (一鍵清理採買清單並同步把第三頁相關膠囊全部亮起為有庫存) ｜ `[ 📋 複製待買清單 ]`。
   - **`[ 📸 拍照/新增食材 ]`**：點擊直接開啟滿版 AI 相機 Step 1 取景視窗，拍攝食品包裝/營養標示，經 Gemini 辨識後建檔入庫生成膠囊！

---

### 3.4 統一 AI 相機拍攝與分析全流程 Modal (Unified Camera Vision Modal)
第二頁與第三頁共用完全相同的 **`CameraVisionModal.vue`**，兩頁點擊後 Step 1 入場畫面 100% 完全相同：

1. **Step 1 取景視窗 (兩頁 100% 相同)**：
   - **滿版相機 View**：畫面上帶有韓式圓角對焦框 🎯。
   - **中央主按鈕**：大拇指位置 `[ ⚪ 拍攝辨識 (快門) ]`。
   - **底部雙備選鈕**：`[ 🖼️ 從相簿選擇照片 ]` ｜ `[ 💬 文字 / 語音輸入 ]`。
2. **Step 2 分析與結果卡片 (依頁面模式分流)**：
   - **第二頁模式 (外食紀錄)**：Gemini 1.5 秒推算整餐熱量/蛋白 ➔ 按下 `[ ✅ 確認寫入今日紀錄 ]` 歸入時間軸。
   - **第三頁模式 (食材建檔)**：Gemini 1.5 秒辨識品名/品牌/每100g數據/自動建議存放區域 ➔ 按下 `[ ✅ 儲存建檔並設為有庫存 ]` 生成第三頁膠囊。

---

## 🎨 4. 全局色彩、圓角與底欄規範 (Global Standards)

1. **卡片圓角 (Rounded Corner)**：大圓角 `16px ~ 20px`，呈現軟質感觸感。
2. **Typography (字體)**：採用高質感 `Noto Sans Rounded` / `Inter`，字重對比鮮明。
3. **全域底部浮動底欄防干擾規範 (Frosted Glass Bottom Action Bar - SSOT)**：
   * **滿版毛玻璃背板**：`background: rgba(250, 248, 245, 0.88); backdrop-filter: blur(16px);`，徹底隔絕滾動時後方卡片與文字干擾。
   * **單行橫排等寬佈局**：所有頁面之底部控制按鈕一律橫向單行（1 Row Flex Layout）等分排列，大拇指單手零負擔點擊。
   * **安全區避讓 (Safe Area)**：內容容器一律提供 `padding-bottom: 100px`，確保頁面滾動到底部時內容 100% 完整露出不被遮擋。
   * **三主頁統一配置**：
     - **第一頁【備料計算器】**：`[ 🍳 計算 ]  [ 🔄 重設 ]  [ 📝 紀錄 ]`
     - **第二頁【今日紀錄】**：`[ 📷 拍照記錄 ]  [ 🎙️ 語音/文字輸入 ]`
     - **第三頁【智慧冰箱】**：`[ 🛒 採買清單 ]  [ 📸 拍照/新增 ]`
